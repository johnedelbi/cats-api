<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">

    <title>cats list</title>
</head>
<body>
    <%- include('./includes/nav')  %>

    <% if(cats.length > 0 ){ %>
        <h1>we found <%= cats.length %> cats</h1>
        <% cats.forEach(cat => { %>
        <div class="cat-container">
            <img src="<%= cat.src  %>" alt="<%= cat.name  %>">
            <h1>cat name: <%= cat.name  %></h1>
            <p>the max lifspan: <%= cat.age  %></p>
            <p>the species: <%= cat.species  %></p>
        </div>
        <% }); } else{ %>
            <h1>there is no cats yet</h1>
            <% } %>
</body>
</html>